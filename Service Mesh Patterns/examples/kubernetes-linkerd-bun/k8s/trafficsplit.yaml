apiVersion: split.smi-spec.io/v1alpha2
kind: TrafficSplit
metadata:
  name: backend
  namespace: canary-mesh
spec:
  service: backend
  backends:
    - service: backend-v1
      weight: 90
    - service: backend-v2
      weight: 10
