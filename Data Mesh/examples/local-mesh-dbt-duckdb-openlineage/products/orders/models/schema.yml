version: 2

models:
  - name: orders_curated
    description: Curated orders table (domain-owned data product)
    columns:
      - name: order_id
        tests: [unique, not_null]
      - name: customer_id
        tests: [not_null]
      - name: order_total_usd
        tests: [not_null]
