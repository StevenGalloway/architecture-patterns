# Demo App (Quarkus)

A tiny HTTP service used for the multi-region demo. It returns region + instance metadata.

In a real system, this would be your API/service container deployed to ECS/Fargate in each region.
